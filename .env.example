# Telegram bot used by /api/booking to deliver new booking requests.
# If either variable is missing, submissions are logged to the server
# console instead of being sent (useful for local development).
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
